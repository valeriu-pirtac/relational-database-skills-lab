[project]
name = "013-backup-disaster-recovery"
version = "0.1.0"
description = "Backup & Disaster Recovery: WAL Archiving, Base Backups, and Point-in-Time Recovery (PITR)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.2",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.2.2",
]
