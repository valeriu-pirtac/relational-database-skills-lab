[project]
name = "016-safe-unsafe-operations"
version = "0.1.0"
description = "Schema Evolution: Safe vs. Unsafe Migrations and Zero-Downtime index and constraint modifications"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.2",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.2.2",
]
