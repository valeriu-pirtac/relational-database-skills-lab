[project]
name = "020-row-level-security"
version = "0.1.0"
description = "Mastering Row-Level Security in PostgreSQL"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.2",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.2.2",
]
